name: shellterm
description: High-performance terminal emulator for Flutter. Zero dependencies.
version: 2.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
